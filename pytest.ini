[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    integration: requires real API credentials
    booking: creates real bookings (use with caution)
